# Normalization Record

The course edition preserves the submitted six-dimension paper-reading
workflow, LaTeX report contract, and proof-tree design.

Public-release changes:

- removed the unsupported `version` key from `SKILL.md` frontmatter;
- changed MinerU from an automatic installation step to an explicit,
  user-approved optional dependency checkpoint;
- added an OpenAI interface file, MIT license copy, provenance, and this
  normalization record;
- recorded the contributor using the course-wide English-name convention.

No papers, generated reports, compiled PDFs, credentials, or third-party
templates are distributed with this package.
